# ============================================================
# PlayBeat TV — Environment Variables (LIVE CONFIG)
# ============================================================
# Download this file, rename to .env, place in project root.
# URL: https://playbeat.live/env-setup.env
# ============================================================

# ---- MongoDB Atlas (PRODUCTION — LIVE & CONNECTED) ----
MONGODB_URI=mongodb+srv://max11:NciH9bevWbkDz5IT@playbeat.umqpdyx.mongodb.net/playbeat?retryWrites=true&w=majority&appName=playbeat

# ---- Database (SQLite fallback — not used with MongoDB) ----
DATABASE_URL=file:./db/custom.db

# ---- NextAuth ----
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=L8obsM1ZC6mxvHgPYcvhmBdR5F5vuQ4uUPm0fB7H3U8=

# ---- Admin Bootstrap (first signup with this email = admin) ----
ADMIN_BOOTSTRAP_EMAIL=admin@playbeat.live

# ---- Google OAuth (fill to enable Google login) ----
# Get from: https://console.cloud.google.com/apis/credentials
# Redirect URI: http://localhost:3000/api/auth/callback/google

# ---- Facebook OAuth (fill to enable Facebook login) ----
# Get from: https://developers.facebook.com/apps
# Redirect URI: http://localhost:3000/api/auth/callback/facebook

# ---- JazzCash (Pakistan — sandbox creds active) ----
JAZZCASH_SANDBOX=true
JAZZCASH_MERCHANT_ID=MC828933
JAZZCASH_PASSWORD=t3zz48w4aa
JAZZCASH_INTEGRITY_SALT=tdw4713y34
JAZZCASH_RETURN_URL=https://playbeat.digital/api/payments/jazzcash/return

# ---- EasyPaisa (Pakistan — manual payment) ----
EASYPAISA_NUMBER=03390005715
NEXT_PUBLIC_EASYPAISA_NUMBER=03390005715

# ---- Stripe (international cards — fill to enable) ----
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# ---- PayPal (international — fill to enable) ----
# Get from: https://developer.paypal.com/dashboard/applications
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_SANDBOX=true

# ---- Bot Handler (30s health checks) ----
BOT_HANDLER_SECRET=playbeat-bot-2026

# ---- Google AdSense ----
# Publisher ID: pub-8143075550797983
# ads.txt already in /public/ads.txt
